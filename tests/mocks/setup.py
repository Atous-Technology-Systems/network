"""RPi.GPIO mock package for development"""
from setuptools import setup, find_packages

setup(
    name="rpi-gpio-mock",
    version="0.1.0",
    description="RPi.GPIO mock for development environments",
    packages=find_packages(),
    python_requires=">=3.8"
)
